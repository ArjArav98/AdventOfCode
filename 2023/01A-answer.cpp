#include<iostream>
#include<fstream>
#include<string>

// in a loop, we basically iterate from start and end
// until they both meet

int main() {
    std::string line;

    std::getline(std::cin, line);
    std::cout<<line<<"+";

    return 0;
}
